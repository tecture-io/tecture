name: Publish @tecture/skill to npm

on:
  push:
    tags:
      - "skill-v*"
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Build + verify the tarball only, do not publish."
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

concurrency:
  group: publish-skill-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Build and publish @tecture/skill
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v5
        with:
          # 22+: the bundled evidence.mjs self-test needs node:sqlite (>= 22.5).
          node-version: 22
          cache: pnpm
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm --filter @tecture/skill typecheck

      - name: Build (bundles the skill payload)
        run: pnpm --filter @tecture/skill build

      - name: Validate bundled skill before publishing
        # Guards against shipping a skill whose own validator is broken.
        run: node packages/installer/dist/skill/architecture-docs/scripts/validate.mjs packages/installer/dist/skill/architecture-docs/reference/example

      - name: Self-test bundled evidence script
        # Guards against shipping a skill whose evidence checker is broken.
        run: node packages/installer/dist/skill/architecture-docs/scripts/evidence.mjs --self-test

      - name: Verify version matches tag
        if: startsWith(github.ref, 'refs/tags/skill-v')
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF#refs/tags/skill-v}"
          PKG_VERSION="$(node -p "require('./packages/installer/package.json').version")"
          echo "tag=$TAG pkg=$PKG_VERSION"
          if [ "$TAG" != "$PKG_VERSION" ]; then
            echo "::error::Tag skill-v$TAG does not match package version $PKG_VERSION"
            exit 1
          fi

      - name: Verify publishable tarball
        run: pnpm --filter @tecture/skill publish --dry-run --no-git-checks

      - name: Publish to npm
        if: inputs.dry-run != 'true'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${NODE_AUTH_TOKEN:-}" ]; then
            echo "::error::NPM_TOKEN secret is not set. Add it in Settings → Secrets and variables → Actions."
            exit 1
          fi
          echo "//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}" > "$HOME/.npmrc"
          pnpm --filter @tecture/skill publish --no-git-checks

      - name: Create GitHub Release
        if: |
          startsWith(github.ref, 'refs/tags/skill-v') &&
          inputs.dry-run != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="${TAG#skill-v}"
          gh release create "$TAG" \
            --title "@tecture/skill $VERSION" \
            --notes "\`npx @tecture/skill@$VERSION\` · see the [repo](https://github.com/${{ github.repository }}#readme)."
